version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    env_file:
      - ../.env.prod
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 30s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "7"
    networks:
      - zczk-network

  backend:
    build:
      context: ../..
      dockerfile: ops/docker/Dockerfile.backend
    env_file:
      - ../.env.prod
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - strapi-uploads:/app/public/uploads
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:1337/api/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "7"
    networks:
      - zczk-network

  frontend:
    build:
      context: ../..
      dockerfile: ops/docker/Dockerfile.frontend
    env_file:
      - ../.env.prod
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:3000/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "7"
    networks:
      - zczk-network

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../nginx/sites.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/www/certbot
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "7"
    networks:
      - zczk-network

  certbot:
    image: certbot/certbot
    env_file:
      - ../.env.prod
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/www/certbot
      - ./init-cert.sh:/opt/init-cert.sh:ro
      - ./certbot-renew.sh:/opt/certbot-renew.sh:ro
    command: ["sleep", "3600"]
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "7"
    networks:
      - zczk-network

volumes:
  postgres-data:
  strapi-uploads:
  certbot-etc:
  certbot-var:

networks:
  zczk-network:
    driver: bridge
